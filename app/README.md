# app/

Contiene rutas y composición de páginas con App Router de Next.js.

## Patrones

- Cada ruta define `metadata` para SEO.
- Las páginas componen componentes desde `components/`.
- El layout global (`layout.tsx`) centraliza navbar, footer y configuración SEO base.

